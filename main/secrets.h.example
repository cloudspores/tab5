// Copy to secrets.h and fill in. secrets.h is gitignored.
#pragma once
#define WIFI_SSID ""
#define WIFI_PASS ""
#define BRIDGE_HOST "my-mac.local"   // the Mac running bridge/server.py: its Bonjour name (System Settings > General > Sharing) or an IP
#define BRIDGE_PORT 8765
